extends layout

block content
  h2 Add a New Word
  form(action="/create", method="POST")
    label(for="word") Word:
    input(type="text", name="word", required)
    br
    label(for="meaning") Meaning:
    input(type="text", name="meaning", required)
    br
    button(type="submit") Add Word
